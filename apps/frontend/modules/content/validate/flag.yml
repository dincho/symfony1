fillin:
  enabled: true
fields:
  comment:
    required:
        msg:    Reason for flagging is required.
    sfStringValidator:
        min:          3
        min_error:    Reason must contain minimum 3 characters.
        max:          255
        max_error:    Reason may contain maximum 255 characters.