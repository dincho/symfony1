propel:
  groups:
    id:
    group_name:          { type: varchar, size: 100, required: true, index: unique }
    group_description:   { type: longvarchar }
      
  permissions:
    id:                 { type: integer, required: true, primaryKey: false, foreignTable: user, foreignReference: id, onDelete: cascade }
    group_id:           { type: integer, required: true, primaryKey: false, foreignTable: groups, foreignReference: id, onDelete: cascade }
          
  group_and_action:
    action:             { type: varchar, size: 200, required: true, primaryKey: true }
    group_id:           { type: integer, required: true, primaryKey: true, foreignTable: groups, foreignReference: id, onDelete: cascade }